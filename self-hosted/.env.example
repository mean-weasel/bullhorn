# Bullhorn Self-Hosted — Supabase Docker Environment
#
# This file documents the key variables you'll need to set in .env
# after running the setup script.
#
# The full .env is copied from the official Supabase Docker setup.
# Below are the most important variables to change:

# REQUIRED — Change these from the defaults!
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your-super-secret-postgres-password
JWT_SECRET=your-super-secret-jwt-token-at-least-32-chars

# Generate these from JWT_SECRET using:
# https://supabase.com/docs/guides/self-hosting/docker#generate-api-keys
ANON_KEY=your-generated-anon-key
SERVICE_ROLE_KEY=your-generated-service-role-key

# Studio dashboard credentials
DASHBOARD_USERNAME=supabase
DASHBOARD_PASSWORD=your-dashboard-password

# Disable features Bullhorn doesn't use
ENABLE_IMAGE_TRANSFORMATION=false
