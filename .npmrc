# Temporary: eslint-plugin-react-hooks@7.0.1 declares peer deps up to ESLint 9,
# but works at runtime with ESLint 10. ESLint 10 support is merged upstream
# (facebook/react#35720) but not yet released. Remove once a stable release ships.
legacy-peer-deps=true
